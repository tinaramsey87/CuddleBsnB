<%= render 'layouts/errors', object: @listing %>

<%= form_for [@city, @listing] do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :zip %>
  <%= f.text_field :zip %>

  <%= f.label :price %>
  <%= f.text_field :price %>

  <%= f.submit %>

<% end %>
